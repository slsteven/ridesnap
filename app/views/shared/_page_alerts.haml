- if flash.any?
  .col-sm-10.col-sm-offset-1.col-lg-8.col-lg-offset-2
    - flash.each do |key, value|
      %div{class: "alert #{flash_class(key)}"}
        = button_tag('&times;'.html_safe, class: 'close', data: {dismiss: 'alert'}, 'aria-hidden' => 'true') unless key == :error
        = value