%p Hello!
%p
  = @user.name
  has decided to become an agent for
  = "#{Settings.app.name}."
%p
  They reside in
  #{@user.zip_code}.
%p
  Reach out to them at
  = @user.email
  or
  = "#{@user.phone}."